@{
    ViewData["Title"] = "Payment Canceled";
}
<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        <h4 class="fw-bold text-warning mb-2">Payment Canceled ⚠️</h4>
        <p class="mb-0">You canceled the payment.</p>
        <a class="btn btn-primary mt-3" asp-controller="BookApplication" asp-action="Index">Back</a>
    </div>
</div>
