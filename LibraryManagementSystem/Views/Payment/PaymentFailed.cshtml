@{
    ViewData["Title"] = "Payment Failed";
}
<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        <h4 class="fw-bold text-danger mb-2">Payment Failed ❌</h4>
        <p class="mb-0">Your payment could not be completed.</p>
        <a class="btn btn-primary mt-3" asp-controller="BookApplication" asp-action="Index">Back</a>
    </div>
</div>
